import { Poppins } from 'next/font/google'

export const text = Poppins({ subsets: ['latin'], weight: '400' })
export const textMedium = Poppins({ subsets: ['latin'], weight: '500' })
export const textBold = Poppins({ subsets: ['latin'], weight: '600' })
